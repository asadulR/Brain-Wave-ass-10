import React from 'react';
import Header from '../Header/Header';
import TopSlider from '../TopSlider/TopSlider';

const Home = () => {
    return (
        <main>
            <Header/>
            <TopSlider/>
        </main>
    );
};

export default Home;